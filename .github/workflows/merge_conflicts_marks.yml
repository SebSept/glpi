name: "Merge conflicts marks"

permissions:
  contents: "read"

on:
  # Runs test suite when a new commit is pushed on "main" and "*/bugfixes" branches
  # and when a new tag is created
  push:
    branches:
      - main
      - 'bugfixes/*'
    tags:
      - '*'
  pull_request:
    branches:
      - main
      - 'bugfixes/*'

jobs:
  check-conflicts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: sv-tools/block-merge-conflicts@v2.0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
